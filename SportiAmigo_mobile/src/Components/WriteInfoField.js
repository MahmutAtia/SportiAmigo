import React from 'react';
import {View, StyleSheet} from 'react-native';

const WriteInfoField = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default WriteInfoField;
